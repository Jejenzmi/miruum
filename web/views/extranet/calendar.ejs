<%- include('../partials/top', { title: 'Kalender Allotment', heading: 'Kalender Rate & Allotment', section:'extranet', active:'dashboard', me }) %>

<% if (saved==='1') { %><div class="notice ok">Kalender diperbarui.</div><% } %>

<div class="card">
  <div style="display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:6px">
    <h2 style="margin:0">Kalender Rate &amp; Allotment</h2>
    <a class="btn ghost sm" href="/extranet/hotels/<%= hotelId %>">← Properti</a>
  </div>
  <%- include('../partials/rate_calendar', { cal: cal, hotelId: hotelId }) %>
</div>

<%- include('../partials/bottom') %>
